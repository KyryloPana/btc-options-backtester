# `/options-backtester` PR #63 compliance audit

This matrix audits the five requested areas against executable code and regression tests. “Complete” means implemented and tested; documentation alone is not credited.

| Request | Implementation | Regression proof | Status |
|---|---|---|---|
| Separate raw generation attempts from unique economic structures. | `retrievedSpreads` remains the complete attempt/provenance collection used to build persisted availability rows. `uniqueCanonicalSpreads` creates the first-seen, deterministic `candidate_id` evaluation/rendering universe; the matrix, runnable observations, selection lookup and scenario input consume that universe. | `generation attempts collapse only at canonical evaluation/rendering boundary`; bundle availability collapse/provenance test. | Complete |
| Evaluate and render each `candidate_id` once while preserving each `availability_id`. | Observation requests and Generated Matrix use `canonicalRetrievedSpreads`; persistence deliberately maps the untouched `retrievedSpreads` to `GenerationCandidateSnapshot` rows. | Canonical-boundary unit test asserts three attempts become two rendered/evaluated structures without mutating the three-row source denominator. | Complete |
| Independent **Hide data-unavailable** Generated Matrix filter. | Separate `hideDataUnavailable` state and checkbox; `visibleMatrixSpreads` composes it independently with Hide red. Both default off. | Matrix filter test covers each filter alone and together; source-presence test checks the control and default state. | Complete |
| Fix Expiry Selection Mode and Pricing Output layout with scoped CSS. | Dedicated `expiry-selection-block` and `pricing-output-block` selectors scope radio layout and a responsive two-column output grid to the construction card only. | Render/source assertions plus production build; selectors do not alter generic radio/checkbox groups. | Complete |
| Reproduce the 2025-07-10 12:00 UTC PnL cliff. | A timestamp-exact fixture includes tempting future IV trades and causal but unsynchronized prior leg evidence. | `2025-07-10 12:00 UTC cliff cannot see future IV or use an unsynchronized leg pair`. | Complete |
| Make IV anchoring causal. | `causalAnchors` accepts exact-instrument trades only at or before the valuation timestamp and within the prior 12 hours. | Cliff test; bounded-IV test; future entry-anchor assertion. | Complete |
| Enforce paired-leg synchronization. | `selectSynchronizedIvAnchors` chooses a deterministic causal pair with at most a 60-minute gap. Path fallback uses both entry anchors together and rejects mixed fresh/stale legs. Direct entry evidence also rejects representative leg gaps over 60 minutes. | Cliff test and independent maker/taker tests. | Complete |
| Expose anchor provenance. | `ModelEvidence` carries trade id, direction, timestamp and age. The point evidence inspector renders both legs’ timestamp, age and trade id. Bundle snapshots retain model evidence. | Research valuation provenance tests and rendered/source assertions. | Complete |
| Invalid points cannot trigger exits or enter statistics. | Missing model points have no PnL. Outcome capture searches only `status:"priced"` points. `researchPathStatistics` includes only priced, finite, point-index-convertible evidence. | Credit-trigger causality tests and point-statistics test with a deliberately extreme missing point. | Complete |
| Use point-level index conversion consistently. | Research table extrema and path statistics call `researchPnlUsd(point)`; the event entry price is no longer used to convert later marks. | Point-statistics fixture uses different index values per point and asserts exact USD extrema. | Complete |
| Separate Worst Observed PnL from MAE. | Detail metrics show both. Worst Observed scans all valid marks; MAE ends at the first profitable valid mark and remains unavailable if profit was not observed. | Point-statistics test proves a later, worse post-profit mark changes Worst Observed but not MAE. | Complete |
| Handle missing entry-inclusive paths honestly. | Statistics require a priced point exactly at entry; otherwise status and reason are `unavailable`, not zero or a later-point substitute. | Point-statistics test removes entry and asserts deterministic unavailability. | Complete |
| Evidence-based statistical inclusion without outcome-based outlier deletion. | Inclusion depends only on timestamp, priced status, finite PnL and a valid point index. No magnitude/outcome filter exists. | Extreme missing point is excluded for missingness; a more extreme valid post-profit point remains included in Worst Observed. | Complete |
