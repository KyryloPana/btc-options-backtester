import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("Duration & DTE workspace exposes required analytical panels",async()=>{const html=await readFile("app/components/duration-dte-report.tsx","utf8");for(const label of ["Duration &amp; DTE","Actual-DTE distribution","Thesis-resolution populations","PnL / MAE by resolution speed","Delayed-entry sensitivity","Decision-rule evaluation","selection-conditional"])assert.ok(html.includes(label),`missing ${label}`);assert.ok(html.includes('data-testid="duration-dte-report"'))});
