export type DisplayCurrency="usd"|"btc";
export function formatDisplayMoney(value:number,currency:DisplayCurrency){const safe=Object.is(value,-0)?0:value;if(currency==="usd")return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",minimumFractionDigits:2,maximumFractionDigits:Math.abs(safe)<.01?6:2}).format(safe);return `${safe.toLocaleString("en-US",{minimumFractionDigits:0,maximumFractionDigits:8})} BTC`}
export function selectDisplayMoney(preferred:DisplayCurrency,values:{btc:number|null;usd:number|null}){const preferredValue=values[preferred];if(preferredValue!==null)return{currency:preferred,value:preferredValue,fallback:false};const other:DisplayCurrency=preferred==="usd"?"btc":"usd",otherValue=values[other];return otherValue===null?null:{currency:other,value:otherValue,fallback:true}}
