import type {AnalysisDataset} from "../research-analysis.ts";import type {AnalysisConfiguration} from "../analysis-configuration.ts";import {datasetForAnalyticsCohort,datasetForAnalyticsTrack,type AnalyticsTrack} from "../research-analytics-model.ts";import {normalizeExitPolicies} from "./normalize.ts";import {EXIT_POLICIES} from "./policies.ts";import {pairedComparison,policyStatistics} from "./statistics.ts";
function layer(dataset:AnalysisDataset,configuration:AnalysisConfiguration,track:AnalyticsTrack){const scope=normalizeExitPolicies(dataset),policies=EXIT_POLICIES.map(policy=>{const observations=scope.filter(x=>x.policyId===policy.id),stats=policyStatistics(observations);return{policy,observations,stats,highlighted:configuration.exitPolicy===policy.id}}),thesis=scope.filter(x=>x.policyId==="thesis");return{scope:{analyticsTrack:track,pricingTrack:null,executionScenario:track==="immediate_maker"?"maker" as const:track==="immediate_taker"?"taker" as const:null},policies,comparisons:policies.filter(x=>x.policy.id!=="thesis").map(x=>({policyId:x.policy.id,...pairedComparison(x.observations,thesis)})),diagnostics:{ties:scope.filter(x=>x.tieResolution).map(x=>x.tieResolution!),missing:scope.filter(x=>x.missingDataReason).map(x=>({candidateId:x.candidateId,policyId:x.policyId,code:x.diagnosticCode,reason:x.missingDataReason}))}}}
export function buildExitPolicyReport(dataset:AnalysisDataset,configuration:AnalysisConfiguration){const selected=datasetForAnalyticsCohort(dataset,"selected"),reference=layer(datasetForAnalyticsTrack(selected,"reference"),configuration,"reference"),maker=layer(datasetForAnalyticsTrack(selected,"immediate_maker"),configuration,"immediate_maker"),taker=layer(datasetForAnalyticsTrack(selected,"immediate_taker"),configuration,"immediate_taker");return{methodology:"Policy chronology and economics use the execution-independent Reference fair-value projection. Immediate Maker and Immediate Taker are separate strict observed-execution robustness layers; missing execution evidence never removes a Reference observation.",scope:reference.scope,nearFullLossFraction:configuration.nearFullLossFraction,policies:reference.policies,comparisons:reference.comparisons,diagnostics:reference.diagnostics,reference,observed:{maker,taker}}}
export type ExitPolicyReport=ReturnType<typeof buildExitPolicyReport>;
