import type {AnalysisConfiguration} from "../analysis-configuration.ts";
import type {Available,PositionEconomics} from "./position.ts";
import {reconstructPortfolio,type PortfolioEconomics} from "./portfolio.ts";

const yes=<T,>(value:T):Available<T>=>({value,reason:null}),no=<T,>(reason:string):Available<T>=>({value:null,reason});
const mean=(v:number[])=>v.length?v.reduce((a,b)=>a+b,0)/v.length:null;
const median=(v:number[])=>{if(!v.length)return null;const x=[...v].sort((a,b)=>a-b),m=x.length>>1;return x.length%2?x[m]!:(x[m-1]!+x[m]!)/2};
const quantile=(v:number[],q:number,min:number):Available<number>=>v.length<min?no(`Not estimable · N=${v.length} · minimum=${min}`):yes([...v].sort((a,b)=>a-b)[Math.floor((v.length-1)*q)]!);
const values=(p:readonly PositionEconomics[],pick:(x:PositionEconomics)=>number|null)=>p.map(pick).filter((x):x is number=>x!==null&&Number.isFinite(x));
export interface ConfigurationEconomics {configurationId:string;configuration:Readonly<Record<string,unknown>>|null;status:"available"|"integrity_error";reason:string|null;conflictingCandidates:readonly {eventId:string;candidateIds:readonly string[]}[];eligibleEvents:number;resolvedStructures:number;priced:number;explicitNoTrade:number;unavailable:number;representedEvents:number;meanPnlBtc:number|null;medianPnlBtc:number|null;meanPnlUsd:number|null;medianPnlUsd:number|null;worstPnlBtc:number|null;p10PnlBtc:Available<number>;p5PnlBtc:Available<number>;medianNetCreditBtc:number|null;medianTotalFeesBtc:number|null;medianFeeDrag:number|null;medianHoldingDays:number|null;medianReturnOnTrackMaximumLoss:number|null;medianReturnOnOpeningMargin:number|null;medianReturnOnPeakCapital:number|null;medianCapitalDaysBtc:number|null;medianCapitalDayReturn:number|null;opportunityExpectancyBtc:Available<number>;positions:readonly PositionEconomics[]}
export function buildConfigurationEconomics(positions:readonly PositionEconomics[],configuration:AnalysisConfiguration):readonly ConfigurationEconomics[]{const grouped=new Map<string,PositionEconomics[]>();for(const p of positions){const id=p.structuralConfigurationId;if(!id)continue;const rows=grouped.get(id)??[];rows.push(p);grouped.set(id,rows)}return [...grouped].sort(([a],[b])=>a.localeCompare(b)).map(([configurationId,rows])=>{const byEvent=new Map<string,PositionEconomics[]>();for(const p of rows){const event=byEvent.get(p.eventId)??[];event.push(p);byEvent.set(p.eventId,event)}const conflicts=[...byEvent].filter(([,x])=>x.length>1).map(([eventId,x])=>({eventId,candidateIds:x.map(p=>p.candidateId).sort()})),unique=conflicts.length?[]:[...byEvent.values()].map(x=>x[0]!),priced=unique.filter(p=>p.status==="priced"),noTrade=unique.filter(p=>p.status==="not_evaluated"),unavailable=unique.filter(p=>p.status==="unavailable"||p.status==="triggered_unpriced"),pnl=values(priced,p=>p.pnlBtc),pnlUsd=values(priced,p=>p.pnlUsd),tailMinimum=Math.max(20,configuration.minimumCellEvents),eligible=byEvent.size,expectancy=unavailable.length?no<number>(`Unavailable evidence exists for ${unavailable.length} of ${eligible} independent MR opportunities; it is not converted to zero.`):yes((pnl.reduce((a,b)=>a+b,0))/eligible);return{configurationId,configuration:rows[0]?.structuralConfiguration??null,status:conflicts.length?"integrity_error":"available",reason:conflicts.length?`Configuration/event identity is not unique for ${conflicts.map(x=>x.eventId).join(", ")}.`:null,conflictingCandidates:conflicts,eligibleEvents:eligible,resolvedStructures:unique.filter(p=>p.entryTimestamp!==null).length,priced:priced.length,explicitNoTrade:noTrade.length,unavailable:unavailable.length,representedEvents:new Set(priced.map(p=>p.eventId)).size,meanPnlBtc:mean(pnl),medianPnlBtc:median(pnl),meanPnlUsd:mean(pnlUsd),medianPnlUsd:median(pnlUsd),worstPnlBtc:pnl.length?Math.min(...pnl):null,p10PnlBtc:quantile(pnl,.1,tailMinimum),p5PnlBtc:quantile(pnl,.05,tailMinimum),medianNetCreditBtc:median(values(priced,p=>p.netOpeningCashFlowBtc)),medianTotalFeesBtc:median(values(priced,p=>p.totalRealizedFeesBtc)),medianFeeDrag:median(values(priced,p=>p.totalRealizedFeesBtc!==null&&p.grossOpeningCreditBtc&&p.grossOpeningCreditBtc>0?p.totalRealizedFeesBtc/p.grossOpeningCreditBtc:null)),medianHoldingDays:median(values(priced,p=>p.holdingDays)),medianReturnOnTrackMaximumLoss:median(values(priced,p=>p.returnOnTrackMaximumNetLoss.value)),medianReturnOnOpeningMargin:median(values(priced,p=>p.returnOnOpeningMargin.value)),medianReturnOnPeakCapital:median(values(priced,p=>p.returnOnPeakCapital.value)),medianCapitalDaysBtc:median(values(priced,p=>p.capitalDaysBtc.value)),medianCapitalDayReturn:median(values(priced,p=>p.pnlBtc!==null&&p.capitalDaysBtc.value!==null&&p.capitalDaysBtc.value>0?p.pnlBtc/p.capitalDaysBtc.value:null)),opportunityExpectancyBtc:conflicts.length?no("Integrity error prevents opportunity expectancy."):expectancy,positions:unique}})}
export function selectedConfigurationPortfolio(summaries:readonly ConfigurationEconomics[],configuration:AnalysisConfiguration):{status:"selected"|"not_selected"|"unavailable";configurationId:string|null;reason:string|null;portfolio:PortfolioEconomics|null}{const id=configuration.selectedStructuralConfigurationId;if(!id)return{status:"not_selected",configurationId:null,reason:"Select a configuration to inspect account chronology.",portfolio:null};const selected=summaries.find(x=>x.configurationId===id);if(!selected)return{status:"unavailable",configurationId:id,reason:"The selected structural configuration is not present in the Q50 cohort.",portfolio:null};if(selected.status==="integrity_error")return{status:"unavailable",configurationId:id,reason:selected.reason,portfolio:null};return{status:"selected",configurationId:id,reason:null,portfolio:reconstructPortfolio(selected.positions.filter(p=>p.status==="priced"),configuration)}}
