import { aggregateRoutedFees, calculateOptionFee, STANDARD_INVERSE_BTC_OPTION_FEE, type ExecutionRoute } from "./accounting.ts";
import { executeHistoricalSpread, type BacktestEvent, type Candle, type ContractSeries, type ContractTrade, type ExecutionMode, type QualityFlag, type RetrievedSpread, type TapeFill } from "./backtester.ts";
import { INVERSE_OPTION_MODEL, priceInverseOption } from "./inverse-option-pricing.ts";
import { expiryPayoff } from "./expiry-payoff.ts";
import { validateOfficialDeliveryPrice } from "./settlement-provenance.ts";

export const RESEARCH_METHODOLOGY_VERSION = "simple-model-reconstruction/1.0.0";
export const RESEARCH_ESTIMATE_DISCLAIMER = "Theoretical estimate; not an actual or confirmed fill and not proof that the requested size could execute.";
export const MODEL_TOOLTIP = "The model reprices each exact option at the target BTC index using a synchronized pair of causal implied-volatility anchors observed at or before that point. It estimates a theoretical mark, not an executable historical quote.";
export const FORWARD_ASSUMPTION = "Forward unavailable — using forward = index and rate = 0";
/** Maximum causal age of an IV observation used by the reference/model reconstruction. */
export const MODEL_IV_ANCHOR_MAX_AGE_MINUTES = 720;
export const RESEARCH_WINDOWS_MINUTES = [30, 60, 120] as const;
/** Declared model methodology; deliberately independent of observed/priced candidates. */
export function modelHistoricalEvidenceWindows(){return{entryMinutes:[MODEL_IV_ANCHOR_MAX_AGE_MINUTES]};}
export type PricingAssumption = "research-estimate" | "conservative-tape-check";
export type EstimateQuality = QualityFlag | "unavailable";
export type ResearchPriceSource = "direct-vwap" | "model-reconstructed";
export type ModelIvSource = "local-observed-IV" | "constant-entry-IV" | "intrinsic-at-expiry" | "unavailable";
export type PathUnavailableReason = "missing-target-index" | "missing-short-entry-iv" | "missing-long-entry-iv" | "short-model-input-invalid" | "long-model-input-invalid" | "model-output-invalid" | "after-expiry" | "contract-unresolved" | "invalid-contract-amount";
export interface ResolvedUnderlyingIndex { index:number; sourceCandleTimestamp:number; lookupMethod:"exact-timestamp"|"containing-candle"|"preceding-completed-candle"; distanceMs:number }

export function expiryWeekday(expiryTimestamp:number){return new Intl.DateTimeFormat("en-US",{weekday:"long",timeZone:"UTC"}).format(new Date(expiryTimestamp));}
export function isFridayExpiry(expiryTimestamp:number){return new Date(expiryTimestamp).getUTCDay()===5;}
export function formatExpiryWithFriday(expiryTimestamp:number|undefined,label?:string){if(expiryTimestamp===undefined)return label??"—";const date=label??new Date(expiryTimestamp).toISOString().slice(0,10);return `${date}${isFridayExpiry(expiryTimestamp)?" · FRI":""}`;}

/** Resolve the close/index used by the trigger engine from hourly open/close keyed candles. */
export function resolveUnderlyingIndex(candles:Candle[],targetTimestamp:number):ResolvedUnderlyingIndex|undefined{
 const ordered=[...candles].filter(c=>Number.isFinite(c.close)&&c.close>0).sort((a,b)=>a.openTime-b.openTime);
 const exact=ordered.find(c=>c.openTime===targetTimestamp||c.closeTime===targetTimestamp);
 if(exact)return{index:exact.close,sourceCandleTimestamp:exact.openTime===targetTimestamp?exact.openTime:exact.closeTime,lookupMethod:"exact-timestamp",distanceMs:0};
 const containing=ordered.find(c=>c.openTime<=targetTimestamp&&targetTimestamp<c.closeTime);
 if(containing)return{index:containing.close,sourceCandleTimestamp:containing.openTime,lookupMethod:"containing-candle",distanceMs:targetTimestamp-containing.openTime};
 const preceding=ordered.filter(c=>c.closeTime<=targetTimestamp).at(-1);if(!preceding)return;
 const interval=Math.max(1,preceding.closeTime-preceding.openTime);const distance=targetTimestamp-preceding.closeTime;
 return distance<=interval?{index:preceding.close,sourceCandleTimestamp:preceding.closeTime,lookupMethod:"preceding-completed-candle",distanceMs:distance}:undefined;
}

export interface ModelEvidence { instrument:string; strike:number; optionType:"C"|"P"; anchorTimestamp:number; anchorTradeId:string|null; anchorDirection:string; anchorAgeMinutes:number; anchorIvApiPercent:number; anchorIvDecimal:number; anchorIndex:number; targetIndex:number; dte:number; forwardPrice:number; rate:number; forwardRateAssumption:string; modelPriceBtc:number; modelName:string }
export interface ResearchLegEstimate { instrumentName:string; economicSide:"sold"|"bought"; priceBtcPerContract:number; unslippedPriceBtcPerContract:number; source:ResearchPriceSource; supportingTrades:ContractTrade[]; supportingTimestamps:number[]; observedAmount:number; nearestGapMinutes:number; model?:ModelEvidence }
export interface ResearchEstimate { valuationMode:"research-estimate"; executionMode:ExecutionMode; intent?:"open"|"close"; pointRole?:"entry"|"scheduled-close"|"outcome-close"|"delayed-entry"|"settlement"; targetTimestamp:number; decisionTimestamp?:number; orderSubmissionTimestamp?:number; evidenceCompletionTimestamp?:number; valuationTimestamp:number; entryTargetIndex:number; status:"priced"; sold:ResearchLegEstimate; bought:ResearchLegEstimate; grossSpreadBtcPerContract:number; grossSpreadBtc:number; openingFeesBtc:number; netOpeningCashFlowBtc:number; amount:number; evidenceWindowMinutes:30|60|120|typeof MODEL_IV_ANCHOR_MAX_AGE_MINUTES; synchronizationGapMinutes:number; priceSource:ResearchPriceSource; estimateQuality:QualityFlag; qualityReason:string; slippageBps:number; observedAmount:number; observedAmountToRequestedRatio:number; rawStaleComparison?:{soldTimestamp:number;boughtTimestamp:number;synchronizationGapMinutes:number;soldPriceBtc:number;boughtPriceBtc:number}; liquidityWarning?:string; amountStepWarning?:string; disclaimer:string }
export interface UnavailableResearchEstimate { valuationMode:"research-estimate";executionMode:ExecutionMode;targetTimestamp:number;status:"unavailable";estimateQuality:"unavailable";reason:string;reasonCode?:string;requestedAmount?:number;shortQualifyingAmount?:number;longQualifyingAmount?:number;shortfall?:number;windowStart?:number;windowEnd?:number;disclaimer:string }
export type ResearchValuation=ResearchEstimate|UnavailableResearchEstimate;
export type StructuralStatus="resolved"|"unresolved";
export type LayerAvailability="available"|"unavailable"|"not_evaluated";
export interface StructuralContractEvaluation { status:StructuralStatus; reasonCode:"exact-contracts-resolved"|"exact-contract-unresolved"; reason:string; missingInstruments:("short"|"long")[]; shortInstrument:string|null; longInstrument:string|null; requestedStrikes:{short:number;long:number}; actualStrikes:{short:number|null;long:number|null}; expiryTimestamp:number|null; listingStatus:string; retrievalError:string|null }
export interface ExecutionLayerEvaluation { status:LayerAvailability; rawVwapStatus:"priced"|"unavailable"|"not_evaluated"; reasonCode:string; reason:string; entry:ResearchValuation; requestedAmount:number; shortQualifyingAmount:number; longQualifyingAmount:number; shortDirection:string; longDirection:string; orderTimestamp:number; fillWindowEnd:number; synchronized:boolean }
export interface ResearchEntryLayers { structural:StructuralContractEvaluation; model:{status:"priced"|"unavailable";reasonCode:string;reason:string;entry:ResearchValuation}; maker:ExecutionLayerEvaluation; taker:ExecutionLayerEvaluation }
export interface EstimatedPathPoint { timestamp:number;status:"priced"|"missing";targetIndex?:number;indexResolution?:ResolvedUnderlyingIndex;unavailableReason?:PathUnavailableReason;rawUnavailableReason?:string;missingField?:string;rawEstimate?:ResearchEstimate;modelEstimate?:ResearchEstimate;/** compatibility alias */ivNormalizedEstimate?:ResearchEstimate;ivSource?:ModelIvSource;soldIvSource?:ModelIvSource;longIvSource?:ModelIvSource;shortIvDecimal?:number;longIvDecimal?:number;shortModelPriceBtc?:number;longModelPriceBtc?:number;closingSpreadValueBtcPerContract?:number;closingSpreadValueBtc?:number;closingFeesBtc?:number;estimatedNetPnlBtc?:number;rawClosingSpreadValueBtcPerContract?:number;rawClosingSpreadValueBtc?:number;rawClosingFeesBtc?:number;rawEstimatedNetPnlBtc?:number;estimateQuality:EstimateQuality }
export type OutcomeUsdUnavailableReason = "outcome-index-unavailable" | "btc-pnl-unavailable";
export interface EstimatedOutcome { label:string;trigger:string;decisionTimestamp?:number;valuationTimestamp?:number;targetIndex?:number;evidenceSource?:string;evidenceReason?:string;ivSource?:ModelIvSource;estimate?:ResearchEstimate;rawEstimate?:number;modelEstimate?:number;feesBtc?:number;estimatedNetPnl?:number;estimatedNetPnlBtc?:number;estimatedNetPnlUsd?:number;conversionIndex?:number;conversionIndexTimestamp?:number;conversionMethod?:"btc-times-outcome-index";usdUnavailableReason?:OutcomeUsdUnavailableReason;estimateQuality:EstimateQuality;status:"estimated"|"not-hit"|"unavailable" }

/**
 * `side` doubles as the action being taken at this moment: "sold" is a sell
 * action (open short at entry, or close long on the path), "bought" is a buy
 * action (open long at entry, or close short on the path) -- callers invert it
 * for path/close valuations, matching backtester.ts's entry/close convention.
 * Trades are filtered to the tape direction consistent with THIS action under
 * THIS execution mode, so a maker-scenario estimate only ever uses evidence of
 * a taker crossing against what would have been our resting order, and a
 * taker-scenario estimate only ever uses evidence of our own side of the tape.
 * Finding no compatible-direction trade is a genuine "not evaluated" for this
 * scenario, not evidence the price was zero.
 */
function directLeg(series:ContractSeries,fill:TapeFill,side:"sold"|"bought",order:number):ResearchLegEstimate|undefined{if(fill.status!=="filled"||fill.tapeVwapBtc===undefined||fill.fillPriceBtc===undefined)return;return{instrumentName:series.instrumentName,economicSide:side,priceBtcPerContract:fill.fillPriceBtc,unslippedPriceBtcPerContract:fill.tapeVwapBtc,source:"direct-vwap",supportingTrades:fill.supportingTrades,supportingTimestamps:fill.supportingTradeTimestamps,observedAmount:fill.filledAmount,nearestGapMinutes:Math.min(...fill.supportingTradeTimestamps.map(t=>(t-order)/60_000))};}

const causalAnchors=(series:ContractSeries,target:number)=>series.trades.filter(t=>t.instrumentName===series.instrumentName&&t.timestamp<=target&&target-t.timestamp<=MODEL_IV_ANCHOR_MAX_AGE_MINUTES*60_000&&t.ivDecimal!==undefined&&t.ivDecimal>0&&Number.isFinite(t.ivDecimal)&&t.indexPrice>0&&Number.isFinite(t.indexPrice)).sort((a,b)=>b.timestamp-a.timestamp||(a.tradeId??"").localeCompare(b.tradeId??""));
/** Latest causal exact-contract anchor. Future prints can never reprice an earlier point. */
export function selectIvAnchor(series:ContractSeries,target:number):ContractTrade|undefined{return causalAnchors(series,target)[0];}
/** Find a causal pair whose evidence timestamps are within the configured one-hour synchronization bound. */
export function selectSynchronizedIvAnchors(short:ContractSeries,long:ContractSeries,target:number,maxGapMs=60*60_000):{short:ContractTrade;long:ContractTrade;gapMinutes:number}|undefined{
 const shorts=causalAnchors(short,target),longs=causalAnchors(long,target);let i=0,j=0;
 while(i<shorts.length&&j<longs.length){const s=shorts[i],l=longs[j],delta=s.timestamp-l.timestamp;if(Math.abs(delta)<=maxGapMs)return{short:s,long:l,gapMinutes:Math.abs(delta)/60_000};if(delta>maxGapMs)i++;else j++;}
}
function modelLeg(series:ContractSeries,target:number,targetIndex:number,side:"sold"|"bought",slippageBps:number,anchorOverride?:ContractTrade):ResearchLegEstimate|undefined{const anchor=anchorOverride??selectIvAnchor(series,target);if(!anchor?.ivDecimal||anchor.timestamp>target)return;const result=priceInverseOption({optionType:series.optionType==="C"?"call":"put",indexPrice:targetIndex,strike:series.strike,valuationTimestamp:target,expiryTimestamp:series.expiryTimestamp,ivDecimal:anchor.ivDecimal});if(result.status==="unavailable")return;const factor=side==="sold"?1-slippageBps/10_000:1+slippageBps/10_000;return{instrumentName:series.instrumentName,economicSide:side,priceBtcPerContract:result.priceBtc*factor,unslippedPriceBtcPerContract:result.priceBtc,source:"model-reconstructed",supportingTrades:[anchor],supportingTimestamps:[anchor.timestamp],observedAmount:anchor.amount,nearestGapMinutes:(target-anchor.timestamp)/60_000,model:{instrument:series.instrumentName,strike:series.strike,optionType:series.optionType,anchorTimestamp:anchor.timestamp,anchorTradeId:anchor.tradeId??null,anchorDirection:anchor.direction,anchorAgeMinutes:(target-anchor.timestamp)/60_000,anchorIvApiPercent:anchor.ivApiPercent??anchor.ivDecimal*100,anchorIvDecimal:anchor.ivDecimal,anchorIndex:anchor.indexPrice,targetIndex,dte:result.timeYears*365,forwardPrice:result.forwardPrice,rate:result.rate,forwardRateAssumption:FORWARD_ASSUMPTION,modelPriceBtc:result.priceBtc,modelName:INVERSE_OPTION_MODEL}};}
function retainModelAnchor(leg:ResearchLegEstimate,series:ContractSeries,target:number,targetIndex:number){
 const anchor=selectIvAnchor(series,target);if(!anchor?.ivDecimal)return leg;
 const modeled=modelLeg(series,target,targetIndex,leg.economicSide,0,anchor);
 return modeled?.model?{...leg,model:modeled.model}:leg;
}
function unavailable(targetTimestamp:number,reason:string,executionMode:ExecutionMode):UnavailableResearchEstimate{return{valuationMode:"research-estimate",executionMode,targetTimestamp,status:"unavailable",estimateQuality:"unavailable",reason,disclaimer:RESEARCH_ESTIMATE_DISCLAIMER};}
function nearestRaw(series:ContractSeries,target:number){return [...series.trades].sort((a,b)=>Math.abs(a.timestamp-target)-Math.abs(b.timestamp-target)||a.timestamp-b.timestamp||(a.tradeId??"").localeCompare(b.tradeId??""))[0];}
function assemble(input:{spread:RetrievedSpread;executionMode:ExecutionMode;targetTimestamp:number;amount:number;slippageBps:number;executionRoute?:ExecutionRoute;targetIndex:number;sold:ResearchLegEstimate;bought:ResearchLegEstimate;window:30|60|120|typeof MODEL_IV_ANCHOR_MAX_AGE_MINUTES;quality:QualityFlag;reason:string;rawStaleComparison?:ResearchEstimate["rawStaleComparison"]}):ResearchEstimate{const {sold,bought}=input;const route=input.executionRoute??"synchronized-leg-proxy";const fees=aggregateRoutedFees([{side:"sell",fee:calculateOptionFee(sold.priceBtcPerContract,input.amount,input.executionMode,STANDARD_INVERSE_BTC_OPTION_FEE)},{side:"buy",fee:calculateOptionFee(bought.priceBtcPerContract,input.amount,input.executionMode,STANDARD_INVERSE_BTC_OPTION_FEE)}],route).finalFee;const per=sold.priceBtcPerContract-bought.priceBtcPerContract,observed=Math.min(sold.observedAmount,bought.observedAmount);return{valuationMode:"research-estimate",executionMode:input.executionMode,targetTimestamp:input.targetTimestamp,valuationTimestamp:input.targetTimestamp,entryTargetIndex:input.targetIndex,status:"priced",sold,bought,grossSpreadBtcPerContract:per,grossSpreadBtc:per*input.amount,openingFeesBtc:fees,netOpeningCashFlowBtc:per*input.amount-fees,amount:input.amount,evidenceWindowMinutes:input.window,synchronizationGapMinutes:Math.abs(sold.supportingTimestamps[0]-bought.supportingTimestamps[0])/60_000,priceSource:sold.source==="direct-vwap"&&bought.source==="direct-vwap"?"direct-vwap":"model-reconstructed",estimateQuality:input.quality,qualityReason:input.reason,slippageBps:input.slippageBps,observedAmount:observed,observedAmountToRequestedRatio:observed/input.amount,rawStaleComparison:input.rawStaleComparison,liquidityWarning:observed<input.amount?"Historical tape does not prove execution of this size.":undefined,amountStepWarning:(!input.spread.soldContract?.amountMetadata||!input.spread.boughtContract?.amountMetadata)?"Exchange increment validation was unavailable; this research scenario is not claimed to be exchange-valid.":undefined,disclaimer:RESEARCH_ESTIMATE_DISCLAIMER};}

/**
 * `executionMode` is required, not defaulted: every call site must say which
 * scenario it is evaluating. Calling this twice (once per mode) against the
 * same spread/timestamp/amount is how a structure is evaluated under both
 * maker opportunity and taker execution -- the two calls never share
 * evidence, and either can independently come back unavailable.
 */
export function estimateResearchSpread(input:{spread:RetrievedSpread;targetTimestamp:number;targetIndex:number;amount?:number;slippageBps:number;executionRoute?:ExecutionRoute;executionMode:ExecutionMode;orderSubmissionTimestamp?:number;fillWindowMinutes?:30|60|120;synchronizationThresholdMs?:number}):ResearchValuation{const amount=input.amount??1,{spread,targetTimestamp,slippageBps,executionMode}=input,order=input.orderSubmissionTimestamp??targetTimestamp;let lastExecution:ReturnType<typeof executeHistoricalSpread>|undefined;if(!Number.isFinite(amount)||amount<=0||!Number.isFinite(slippageBps)||slippageBps<0||!Number.isFinite(input.targetIndex)||input.targetIndex<=0)return unavailable(targetTimestamp,"Essential numerical inputs are invalid.",executionMode);if(spread.dataStatus==="data-unavailable"||spread.retrievalStatus==="partial"||!spread.soldContract||!spread.boughtContract||spread.retrievalStatus!=="ready")return unavailable(targetTimestamp,"Both exact contracts must be resolved.",executionMode);const creditOrUnavailable=(estimate:ResearchEstimate):ResearchValuation=>estimate.grossSpreadBtcPerContract>0?estimate:unavailable(targetTimestamp,estimate.grossSpreadBtcPerContract===0?"Scenario-specific entry evidence implies zero gross credit; a credit spread requires strictly positive credit.":"Scenario-specific entry evidence implies a debit, not a credit.",executionMode);for(const window of input.fillWindowMinutes?[input.fillWindowMinutes]:RESEARCH_WINDOWS_MINUTES){const execution=executeHistoricalSpread({spread,intent:"open",executionMode,decisionTimestamp:targetTimestamp,orderSubmissionTimestamp:order,fillWindowMs:window*60_000,amount,maxLegSyncMs:input.synchronizationThresholdMs??60*60_000,slippageBps});lastExecution=execution;const directSold=directLeg(spread.soldContract,execution.sold,"sold",order),directBought=directLeg(spread.boughtContract,execution.bought,"bought",order);if(execution.status!=="no-trade"&&directSold&&directBought){const sold=retainModelAnchor(directSold,spread.soldContract,targetTimestamp,input.targetIndex),bought=retainModelAnchor(directBought,spread.boughtContract,targetTimestamp,input.targetIndex);const sync=(execution.synchronizationGapMs??0)/60_000;const quality:QualityFlag=window===30&&sync<=30?"green":"yellow";const estimate=assemble({...input,amount,sold,bought,window,quality,reason:executionMode==="maker"?"Maker opportunity has sufficient opposing-aggressor volume; tape cannot prove queue priority.":`Both legs use bounded causal taker evidence after order submission.`});return creditOrUnavailable({...estimate,intent:"open",pointRole:"entry",decisionTimestamp:targetTimestamp,orderSubmissionTimestamp:order,evidenceCompletionTimestamp:execution.completionTimestamp,valuationTimestamp:execution.completionTimestamp??order});}}
 if(lastExecution&&(lastExecution.sold.compatibleAmountBeforeSearchEnd>0||lastExecution.bought.compatibleAmountBeforeSearchEnd>0)){const result=unavailable(targetTimestamp,"Requested size lacks sufficient qualifying causal tape on both exact legs.",executionMode);return{...result,reasonCode:"insufficient-compatible-amount",requestedAmount:amount,shortQualifyingAmount:lastExecution.sold.compatibleAmountBeforeSearchEnd,longQualifyingAmount:lastExecution.bought.compatibleAmountBeforeSearchEnd,shortfall:Math.max(amount-lastExecution.sold.compatibleAmountBeforeSearchEnd,amount-lastExecution.bought.compatibleAmountBeforeSearchEnd,0),windowStart:order,windowEnd:lastExecution.clock.fillSearchEnd};}
 // No compatible-direction trade was found in any direct-evidence window for
 // this scenario. Falling through to the IV-anchored model reprice is
 // unchanged behavior; it never claims to prove this scenario's executability
 // (estimateQuality stays "red"), so it does not substitute one scenario's
 // absence with a confident number.
 const anchors=selectSynchronizedIvAnchors(spread.soldContract,spread.boughtContract,targetTimestamp,input.synchronizationThresholdMs??60*60_000);if(!anchors)return unavailable(targetTimestamp,"Either exact leg lacks a causal IV-anchor pair synchronized within 60 minutes.",executionMode);const sold=modelLeg(spread.soldContract,targetTimestamp,input.targetIndex,"sold",slippageBps,anchors.short),bought=modelLeg(spread.boughtContract,targetTimestamp,input.targetIndex,"bought",slippageBps,anchors.long);if(!sold||!bought)return unavailable(targetTimestamp,"Either exact leg lacks a valid causal IV/index anchor within 12 hours.",executionMode);const rawSold=nearestRaw(spread.soldContract,targetTimestamp),rawBought=nearestRaw(spread.boughtContract,targetTimestamp);const rawStaleComparison=rawSold&&rawBought?{soldTimestamp:rawSold.timestamp,boughtTimestamp:rawBought.timestamp,synchronizationGapMinutes:Math.abs(rawSold.timestamp-rawBought.timestamp)/60_000,soldPriceBtc:rawSold.price,boughtPriceBtc:rawBought.price}:undefined;return creditOrUnavailable(assemble({...input,amount,sold,bought,window:MODEL_IV_ANCHOR_MAX_AGE_MINUTES,quality:"red",reason:"Exact contracts were synchronized by theoretical reconstruction from bounded causal IV anchors; quality remains Red.",rawStaleComparison}));}


/** The execution-independent theoretical entry. It never consumes fill/VWAP evidence. */
export function estimateModelSpread(input:{spread:RetrievedSpread;targetTimestamp:number;targetIndex:number;amount?:number;slippageBps:number;synchronizationThresholdMs?:number}):ResearchValuation{
 const {spread,targetTimestamp,targetIndex}=input,amount=input.amount??1;
 if(!spread.soldContract||!spread.boughtContract||spread.retrievalStatus!=="ready"||spread.dataStatus==="data-unavailable")return unavailable(targetTimestamp,"Both exact contracts must be resolved before model valuation.","taker");
 const anchors=selectSynchronizedIvAnchors(spread.soldContract,spread.boughtContract,targetTimestamp,input.synchronizationThresholdMs??60*60_000);
 if(!anchors)return{...unavailable(targetTimestamp,"A synchronized causal IV-anchor pair is unavailable.","taker"),reasonCode:"causal-iv-anchor-pair-unavailable"};
 const sold=modelLeg(spread.soldContract,targetTimestamp,targetIndex,"sold",input.slippageBps,anchors.short),bought=modelLeg(spread.boughtContract,targetTimestamp,targetIndex,"bought",input.slippageBps,anchors.long);
 if(!sold||!bought)return{...unavailable(targetTimestamp,"Causal IV anchors do not provide valid model inputs.","taker"),reasonCode:"model-input-invalid"};
 const estimate=assemble({spread,executionMode:"taker",targetTimestamp,targetIndex,amount,slippageBps:input.slippageBps,sold,bought,window:MODEL_IV_ANCHOR_MAX_AGE_MINUTES,quality:"red",reason:"Theoretical IV-normalized valuation from synchronized causal anchors; independent of execution."});
 return estimate.grossSpreadBtcPerContract>0?estimate:{...unavailable(targetTimestamp,"Model valuation does not imply a positive credit.","taker"),reasonCode:"model-non-credit"};
}

/** Evaluate construction, theoretical value, maker tape and taker tape without inference between layers. */
export function evaluateResearchEntryLayers(input:{spread:RetrievedSpread;targetTimestamp:number;targetIndex:number;amount:number;slippageBps:number;executionRoute?:ExecutionRoute;orderSubmissionTimestamp?:number;fillWindowMinutes?:30|60|120;synchronizationThresholdMs?:number}):ResearchEntryLayers{
 const {spread}=input,order=input.orderSubmissionTimestamp??input.targetTimestamp,window=input.fillWindowMinutes??120;
 const missing:("short"|"long")[]=[];if(!spread.soldContract)missing.push("short");if(!spread.boughtContract)missing.push("long");
 const unresolved=missing.length>0||spread.retrievalStatus!=="ready"||spread.dataStatus==="data-unavailable";
 const structural:StructuralContractEvaluation={status:unresolved?"unresolved":"resolved",reasonCode:unresolved?"exact-contract-unresolved":"exact-contracts-resolved",reason:missing.length?`Missing exact ${missing.join(" and ")} instrument.`:unresolved?(spread.retrievalNote||"Exact listing identity or retrieval is incomplete."):"Both exact instruments and expiry are resolved.",missingInstruments:missing,shortInstrument:spread.soldContract?.instrumentName??null,longInstrument:spread.boughtContract?.instrumentName??null,requestedStrikes:{short:spread.soldStrike,long:spread.boughtStrike},actualStrikes:{short:spread.resolvedSoldStrike??null,long:spread.resolvedBoughtStrike??null},expiryTimestamp:spread.expiryTimestamp??null,listingStatus:spread.retrievalStatus,retrievalError:spread.retrievalNote||null};
 const modelEntry=!unresolved?estimateModelSpread(input):{...unavailable(input.targetTimestamp,"Exact contracts are unresolved.","taker"),reasonCode:"exact-contract-unresolved"};
 const scenario=(mode:ExecutionMode):ExecutionLayerEvaluation=>{
  if(unresolved){const entry={...unavailable(input.targetTimestamp,"Exact contracts are unresolved.",mode),reasonCode:"exact-contract-unresolved"};return{status:"unavailable",rawVwapStatus:"unavailable",reasonCode:"exact-contract-unresolved",reason:entry.reason,entry,requestedAmount:input.amount,shortQualifyingAmount:0,longQualifyingAmount:0,shortDirection:mode==="maker"?"opposing buy aggressor":"sell aggressor",longDirection:mode==="maker"?"opposing sell aggressor":"buy aggressor",orderTimestamp:order,fillWindowEnd:order+window*60_000,synchronized:false};}
  const evaluated=estimateResearchSpread({...input,executionMode:mode,fillWindowMinutes:window});
  const raw:ResearchValuation=evaluated.status==="priced"&&evaluated.priceSource==="direct-vwap"?evaluated:evaluated.status==="unavailable"?evaluated:{...unavailable(input.targetTimestamp,"No qualifying causal raw VWAP was available.",mode),reasonCode:"raw-vwap-unavailable"};
  const available=raw.status==="priced";return{status:available?"available":"unavailable",rawVwapStatus:available?"priced":"unavailable",reasonCode:available?"qualifying-causal-vwap":raw.reasonCode??"raw-vwap-unavailable",reason:available?raw.qualityReason:raw.reason,entry:raw,requestedAmount:input.amount,shortQualifyingAmount:raw.status==="unavailable"?raw.shortQualifyingAmount??0:raw.sold.observedAmount,longQualifyingAmount:raw.status==="unavailable"?raw.longQualifyingAmount??0:raw.bought.observedAmount,shortDirection:mode==="maker"?"opposing buy aggressor":"sell aggressor",longDirection:mode==="maker"?"opposing sell aggressor":"buy aggressor",orderTimestamp:order,fillWindowEnd:raw.status==="unavailable"?raw.windowEnd??order+window*60_000:raw.evidenceCompletionTimestamp??order+window*60_000,synchronized:available};
 };
 return{structural,model:{status:modelEntry.status==="priced"?"priced":"unavailable",reasonCode:modelEntry.status==="priced"?"synchronized-causal-iv-model":modelEntry.reasonCode??"model-unavailable",reason:modelEntry.status==="priced"?modelEntry.qualityReason:modelEntry.reason,entry:modelEntry},maker:scenario("maker"),taker:scenario("taker")};
}

function modelMark(input:{spread:RetrievedSpread;timestamp:number;index:number;entry:ResearchEstimate;slippageBps:number;executionRoute?:ExecutionRoute}):{estimate?:ResearchEstimate;source:ModelIvSource;soldSource:ModelIvSource;longSource:ModelIvSource;reason?:PathUnavailableReason}{
 const {spread,timestamp,index,entry}=input;if(!spread.soldContract||!spread.boughtContract)return{source:"unavailable",soldSource:"unavailable",longSource:"unavailable",reason:"contract-unresolved"};if(timestamp>spread.soldContract.expiryTimestamp)return{source:"unavailable",soldSource:"unavailable",longSource:"unavailable",reason:"after-expiry"};
 const expired=timestamp===spread.soldContract.expiryTimestamp,pair=selectSynchronizedIvAnchors(spread.soldContract,spread.boughtContract,timestamp);let soldAnchor=pair?.short,boughtAnchor=pair?.long;let soldSource:ModelIvSource=pair?"local-observed-IV":"constant-entry-IV",longSource:ModelIvSource=soldSource;
 // Fallback is paired and entry-causal: never mix a fresh leg with a stale leg.
 if(!pair&&entry.sold.model&&entry.bought.model){soldAnchor={...entry.sold.supportingTrades[0],timestamp:entry.sold.model.anchorTimestamp,indexPrice:entry.sold.model.anchorIndex,ivDecimal:entry.sold.model.anchorIvDecimal,ivApiPercent:entry.sold.model.anchorIvApiPercent,instrumentName:entry.sold.model.instrument};boughtAnchor={...entry.bought.supportingTrades[0],timestamp:entry.bought.model.anchorTimestamp,indexPrice:entry.bought.model.anchorIndex,ivDecimal:entry.bought.model.anchorIvDecimal,ivApiPercent:entry.bought.model.anchorIvApiPercent,instrumentName:entry.bought.model.instrument};if(Math.abs(soldAnchor.timestamp-boughtAnchor.timestamp)>60*60_000||soldAnchor.timestamp>timestamp||boughtAnchor.timestamp>timestamp){soldAnchor=boughtAnchor=undefined;}}
 if(!soldAnchor?.ivDecimal)return{source:"unavailable",soldSource,longSource,reason:"missing-short-entry-iv"};if(!boughtAnchor?.ivDecimal)return{source:"unavailable",soldSource,longSource,reason:"missing-long-entry-iv"};if(expired)soldSource=longSource="intrinsic-at-expiry";
 const sold=modelLeg(spread.soldContract,timestamp,index,"bought",input.slippageBps,soldAnchor);if(!sold)return{source:"unavailable",soldSource,longSource,reason:"short-model-input-invalid"};const bought=modelLeg(spread.boughtContract,timestamp,index,"sold",input.slippageBps,boughtAnchor);if(!bought)return{source:"unavailable",soldSource,longSource,reason:"long-model-input-invalid"};const source:ModelIvSource=expired?"intrinsic-at-expiry":soldSource;return{source,soldSource,longSource,estimate:assemble({...input,executionMode:entry.executionMode,targetIndex:index,targetTimestamp:timestamp,amount:entry.amount,sold,bought,window:MODEL_IV_ANCHOR_MAX_AGE_MINUTES,quality:"red",reason:expired?"Intrinsic value at expiry.":`${soldSource} synchronized pair.`})};
}
export function validateResearchAmount(amount:number){return Number.isFinite(amount)&&amount>0?undefined:"Contract amount must be a finite number greater than zero.";}
export function buildEstimatedPath(input:{spread:RetrievedSpread;timestamps:number[];indexAt?:(timestamp:number)=>number|undefined;candles?:Candle[];entry:ResearchEstimate;slippageBps:number;executionRoute?:ExecutionRoute}):EstimatedPathPoint[]{const expiry=input.spread.expiryTimestamp;return [...new Set(input.timestamps)].filter(t=>expiry===undefined||t<=expiry).sort((a,b)=>a-b).map(timestamp=>{const resolution=input.candles?resolveUnderlyingIndex(input.candles,timestamp):undefined;const index=timestamp===expiry&&input.spread.deliveryPrice!==undefined?input.spread.deliveryPrice:resolution?.index??input.indexAt?.(timestamp);if(index===undefined||!Number.isFinite(index)||index<=0)return{timestamp,status:"missing",unavailableReason:"missing-target-index",missingField:"targetIndex",ivSource:"unavailable",estimateQuality:"unavailable"};
 const modeled=modelMark({...input,timestamp,index}),model=modeled.estimate;const execution=executeHistoricalSpread({spread:input.spread,intent:"close",executionMode:input.entry.executionMode,decisionTimestamp:timestamp,orderSubmissionTimestamp:timestamp,fillWindowMs:120*60_000,amount:input.entry.amount,maxLegSyncMs:60*60_000,slippageBps:input.slippageBps});
 let raw:ResearchEstimate|undefined,rawClosingSpreadValueBtcPerContract:number|undefined,rawClosingSpreadValueBtc:number|undefined,rawClosingFeesBtc:number|undefined,rawEstimatedNetPnlBtc:number|undefined;
 if(execution.status!=="no-trade"&&input.spread.soldContract&&input.spread.boughtContract){const sold=directLeg(input.spread.soldContract,execution.sold,"sold",timestamp),bought=directLeg(input.spread.boughtContract,execution.bought,"bought",timestamp);if(sold&&bought){raw=assemble({spread:input.spread,executionMode:input.entry.executionMode,targetTimestamp:timestamp,targetIndex:index,amount:input.entry.amount,slippageBps:input.slippageBps,executionRoute:input.executionRoute,sold,bought,window:120,quality:(execution.synchronizationGapMs??Infinity)<=30*60_000?"green":"yellow",reason:"Both exact legs use sufficient, synchronized, scenario-local closing-side tape after the scheduled close order."});raw={...raw,intent:"close",pointRole:"scheduled-close",decisionTimestamp:timestamp,orderSubmissionTimestamp:timestamp,evidenceCompletionTimestamp:execution.completionTimestamp,valuationTimestamp:execution.completionTimestamp??timestamp};rawClosingSpreadValueBtcPerContract=bought.priceBtcPerContract-sold.priceBtcPerContract;rawClosingSpreadValueBtc=rawClosingSpreadValueBtcPerContract*input.entry.amount;rawClosingFeesBtc=raw.openingFeesBtc;rawEstimatedNetPnlBtc=input.entry.netOpeningCashFlowBtc+rawClosingSpreadValueBtc-rawClosingFeesBtc;}}
 const reason=modeled.reason??"model-output-invalid",missingField=reason==="missing-short-entry-iv"?"entry.sold.model.anchorIvDecimal":reason==="missing-long-entry-iv"?"entry.bought.model.anchorIvDecimal":reason==="short-model-input-invalid"?"shortModelInput":reason==="long-model-input-invalid"?"longModelInput":undefined;
 if(!raw&&!model)return{timestamp,targetIndex:index,indexResolution:resolution,status:"missing",unavailableReason:reason,rawUnavailableReason:execution.reason,missingField,ivSource:"unavailable",soldIvSource:modeled.soldSource,longIvSource:modeled.longSource,estimateQuality:"unavailable"};
 const shortModelPriceBtc=model?.sold.priceBtcPerContract,longModelPriceBtc=model?.bought.priceBtcPerContract,closingSpreadValueBtcPerContract=model&&longModelPriceBtc!-shortModelPriceBtc!,closingSpreadValueBtc=closingSpreadValueBtcPerContract===undefined?undefined:closingSpreadValueBtcPerContract*input.entry.amount,closingFeesBtc=model?.openingFeesBtc,pnl=closingSpreadValueBtc===undefined||closingFeesBtc===undefined?undefined:input.entry.netOpeningCashFlowBtc+closingSpreadValueBtc-closingFeesBtc;
 return{timestamp,targetIndex:index,indexResolution:resolution,status:"priced",rawEstimate:raw,rawUnavailableReason:raw?undefined:execution.reason,rawClosingSpreadValueBtcPerContract,rawClosingSpreadValueBtc,rawClosingFeesBtc,rawEstimatedNetPnlBtc,modelEstimate:model,ivNormalizedEstimate:model,ivSource:model?modeled.source:"unavailable",soldIvSource:modeled.soldSource,longIvSource:modeled.longSource,shortIvDecimal:model?.sold.model?.anchorIvDecimal,longIvDecimal:model?.bought.model?.anchorIvDecimal,shortModelPriceBtc,longModelPriceBtc,closingSpreadValueBtcPerContract,closingSpreadValueBtc,closingFeesBtc,estimatedNetPnlBtc:pnl,estimateQuality:raw?.estimateQuality??(model?"red":"unavailable")};});}
export function researchOutcomeAt(label:string,trigger:string,timestamp:number|undefined,path:EstimatedPathPoint[]):EstimatedOutcome{if(timestamp===undefined)return{label,trigger,status:"unavailable",estimateQuality:"unavailable",evidenceSource:"unavailable",evidenceReason:"The bounded trigger timestamp is missing."};const point=[...path].filter(p=>p.timestamp>=timestamp).sort((a,b)=>a.timestamp-b.timestamp)[0];if(!point||point.status!=="priced"||point.estimatedNetPnlBtc===undefined)return{label,trigger,decisionTimestamp:timestamp,valuationTimestamp:point?.timestamp,status:"unavailable",estimateQuality:"unavailable",evidenceSource:"unavailable",evidenceReason:"No model valuation is available at or after this outcome timestamp.",usdUnavailableReason:"btc-pnl-unavailable"};const estimate=point.rawEstimate??point.modelEstimate??point.ivNormalizedEstimate,pnlBtc=point.estimatedNetPnlBtc,index=point.targetIndex;return{label,trigger,decisionTimestamp:timestamp,valuationTimestamp:point.timestamp,targetIndex:index,status:"estimated",estimateQuality:point.estimateQuality,evidenceSource:estimate?.priceSource,evidenceReason:estimate?.qualityReason,ivSource:point.ivSource,estimate,modelEstimate:estimate?.grossSpreadBtc,feesBtc:estimate?.openingFeesBtc,estimatedNetPnl:pnlBtc,estimatedNetPnlBtc:pnlBtc,estimatedNetPnlUsd:index===undefined?undefined:pnlBtc*index,conversionIndex:index,conversionIndexTimestamp:index===undefined?undefined:point.timestamp,conversionMethod:index===undefined?undefined:"btc-times-outcome-index",usdUnavailableReason:index===undefined?"outcome-index-unavailable":undefined};}
export function buildResearchOutcomes(input:{event:BacktestEvent;spread:RetrievedSpread;entry:ResearchEstimate;path:EstimatedPathPoint[];candles:Candle[]}):EstimatedOutcome[]{const {event,spread,entry,path,candles}=input,start=entry.targetTimestamp;const priced=path.filter(p=>p.status==="priced"&&p.estimatedNetPnlBtc!==undefined);const capture=(threshold:number)=>priced.find(point=>{const estimate=point.rawEstimate??point.modelEstimate??point.ivNormalizedEstimate;if(!estimate||entry.grossSpreadBtc<=0)return false;return(entry.grossSpreadBtc+(estimate.bought.priceBtcPerContract-estimate.sold.priceBtcPerContract)*entry.amount)/entry.grossSpreadBtc>=threshold;});const invalidation=event.invalidationPrice===undefined?undefined:completedCandleTrigger(candles,start,c=>event.direction==="long"?c.close<event.invalidationPrice!:c.close>event.invalidationPrice!);const fixed=(days:number)=>{const timestamp=start+days*864e5;return spread.expiryTimestamp!==undefined&&timestamp>spread.expiryTimestamp?{label:`${days}D`,trigger:`${days} days after entry`,decisionTimestamp:timestamp,status:"unavailable" as const,estimateQuality:"unavailable" as const,evidenceSource:"unavailable",evidenceReason:"Target occurs after contract expiry.",usdUnavailableReason:"btc-pnl-unavailable" as const}:researchOutcomeAt(`${days}D`,`${days} days after entry`,timestamp,path);};const outcomes=[researchOutcomeAt("VPOC","Bundled VPOC timestamp",event.vpocTimestamp,path),researchOutcomeAt("25% credit","First bounded mark at 25% credit capture",capture(.25)?.timestamp,path),researchOutcomeAt("50% credit","First bounded mark at 50% credit capture",capture(.5)?.timestamp,path),researchOutcomeAt("70% credit","First bounded mark at 70% credit capture",capture(.7)?.timestamp,path),fixed(3),fixed(5),fixed(7),researchOutcomeAt("Invalidation","First completed 1H candle beyond invalidation",invalidation?.closeTime,path)];const expiry=spread.expiryTimestamp,delivery=validateOfficialDeliveryPrice(spread);if(expiry===undefined||!delivery.ok||!spread.soldContract||!spread.boughtContract)return[...outcomes,{label:"Settlement",trigger:"Deribit delivery price",decisionTimestamp:expiry,status:"unavailable",estimateQuality:"unavailable",evidenceSource:"unavailable",evidenceReason:delivery.ok?"Resolved contracts are required.":delivery.reason,usdUnavailableReason:"outcome-index-unavailable"}];const payoff=expiryPayoff({optionType:spread.soldContract.optionType,shortStrike:spread.soldContract.strike,longStrike:spread.boughtContract.strike,shortEntryPremiumBtc:entry.sold.priceBtcPerContract,longEntryPremiumBtc:entry.bought.priceBtcPerContract,entryIndex:entry.entryTargetIndex,amount:entry.amount,openingFeesBtc:entry.openingFeesBtc,expiryTimestamp:expiry},delivery.deliveryPrice,"btc-settlement");const pnlBtc=payoff.pnl;return[...outcomes,{label:"Settlement",trigger:"Deribit delivery price",decisionTimestamp:expiry,valuationTimestamp:expiry,targetIndex:delivery.deliveryPrice,status:"estimated",estimateQuality:"green",ivSource:"intrinsic-at-expiry",evidenceSource:"deribit-delivery-price",evidenceReason:`Versioned intrinsic settlement at ${delivery.deliveryPrice}; delivery fees ${payoff.settlementFeesBtc} BTC.`,feesBtc:payoff.settlementFeesBtc,estimatedNetPnl:pnlBtc,estimatedNetPnlBtc:pnlBtc,estimatedNetPnlUsd:entry.netOpeningCashFlowBtc*entry.entryTargetIndex+(payoff.netPositionValueBtc)*delivery.deliveryPrice,conversionIndex:delivery.deliveryPrice,conversionIndexTimestamp:expiry,conversionMethod:"btc-times-outcome-index"}];}
export function scaleResearchEstimate(entry:ResearchEstimate,amount:number,executionRoute?:ExecutionRoute):ResearchEstimate{const fees=aggregateRoutedFees([{side:"sell",fee:calculateOptionFee(entry.sold.priceBtcPerContract,amount,entry.executionMode,STANDARD_INVERSE_BTC_OPTION_FEE)},{side:"buy",fee:calculateOptionFee(entry.bought.priceBtcPerContract,amount,entry.executionMode,STANDARD_INVERSE_BTC_OPTION_FEE)}],executionRoute??"synchronized-leg-proxy").finalFee,gross=entry.grossSpreadBtcPerContract*amount;return{...entry,amount,grossSpreadBtc:gross,openingFeesBtc:fees,netOpeningCashFlowBtc:gross-fees,observedAmountToRequestedRatio:entry.observedAmount/amount,liquidityWarning:entry.observedAmount<amount?"Historical tape does not prove execution of this size.":undefined};}
export function scaleResearchPath(path:EstimatedPathPoint[],entry:ResearchEstimate,amount:number,executionRoute?:ExecutionRoute):EstimatedPathPoint[]{const scaledEntry=scaleResearchEstimate(entry,amount,executionRoute);return path.map(point=>{const mark=point.rawEstimate??point.modelEstimate??point.ivNormalizedEstimate;if(point.status!=="priced"||!mark)return{...point,estimatedNetPnlBtc:undefined};const scaled=scaleResearchEstimate(mark,amount,executionRoute),closingPerContract=point.closingSpreadValueBtcPerContract??scaled.bought.priceBtcPerContract-scaled.sold.priceBtcPerContract,closing=closingPerContract*amount;return{...point,rawEstimate:point.rawEstimate?scaled:undefined,modelEstimate:point.modelEstimate?scaled:undefined,ivNormalizedEstimate:point.ivNormalizedEstimate?scaled:undefined,closingSpreadValueBtc:closing,closingFeesBtc:scaled.openingFeesBtc,estimatedNetPnlBtc:scaledEntry.netOpeningCashFlowBtc+closing-scaled.openingFeesBtc};});}
export function completedCandleTrigger(candles:Candle[],after:number,predicate:(c:Candle)=>boolean){return[...candles].sort((a,b)=>a.closeTime-b.closeTime).find(c=>c.closeTime>after&&predicate(c));}
export const OPENING_CONVERSION_METHOD = "btc-times-entry-target-index" as const;
export function openingUsdEquivalent(valueBtc:number|undefined,entryTargetIndex:number|undefined){if(valueBtc===undefined||entryTargetIndex===undefined||!Number.isFinite(valueBtc)||!Number.isFinite(entryTargetIndex)||entryTargetIndex<=0)return undefined;return valueBtc*entryTargetIndex;}

export function buildResearchExport(input:{event:BacktestEvent;spread:RetrievedSpread;entry:ResearchValuation;path?:EstimatedPathPoint[];outcomes?:EstimatedOutcome[]}){return{methodology:{version:RESEARCH_METHODOLOGY_VERSION,pricingModel:INVERSE_OPTION_MODEL,formulaConvention:"Deribit inverse-option Black–Scholes; USD theoretical value divided by target BTC index",ivUnits:{api:"percent",internal:"ivDecimal"},hierarchy:["direct amount-weighted VWAP in ±30 minutes","direct amount-weighted VWAP in ±1 hour","direct amount-weighted VWAP in ±2 hours","model-reconstructed exact-contract price using synchronized causal IV anchors from the prior 12 hours"],forwardRateAssumption:FORWARD_ASSUMPTION,missingPointPolicy:"constant-entry-IV after entry; unavailable when index or entry IV is missing"},mode:"research-estimate" as const,eventId:input.event.id,expiryWeekday:input.spread.expiryTimestamp===undefined?undefined:expiryWeekday(input.spread.expiryTimestamp),isFridayExpiry:input.spread.expiryTimestamp===undefined?false:isFridayExpiry(input.spread.expiryTimestamp),contracts:{sold:input.spread.soldContract?.instrumentName,bought:input.spread.boughtContract?.instrumentName,soldStrike:input.spread.soldContract?.strike,boughtStrike:input.spread.boughtContract?.strike,expiryTimestamp:input.spread.expiryTimestamp,expiryWeekday:input.spread.expiryTimestamp===undefined?undefined:expiryWeekday(input.spread.expiryTimestamp),isFridayExpiry:input.spread.expiryTimestamp===undefined?false:isFridayExpiry(input.spread.expiryTimestamp)},windowsUsed:input.entry.status==="priced"?[input.entry.evidenceWindowMinutes]:[],priceSource:input.entry.status==="priced"?input.entry.priceSource:"unavailable",modelEvidence:input.entry.status==="priced"?{sold:input.entry.sold.model,bought:input.entry.bought.model}:undefined,quality:input.entry.estimateQuality,slippageAssumptionBps:input.entry.status==="priced"?input.entry.slippageBps:undefined,feesBtc:input.entry.status==="priced"?input.entry.openingFeesBtc:undefined,sourceClassification:input.entry.status==="priced"?input.entry.priceSource:"unavailable",estimateDisclaimer:RESEARCH_ESTIMATE_DISCLAIMER,entryTargetIndex:input.entry.status==="priced"?input.entry.entryTargetIndex:undefined,openingCurrencyConversion:input.entry.status==="priced"?{method:OPENING_CONVERSION_METHOD,timestamp:input.entry.targetTimestamp,index:input.entry.entryTargetIndex}:undefined,openingUsdEquivalent:input.entry.status==="priced"?{soldPremium:openingUsdEquivalent(input.entry.sold.priceBtcPerContract*input.entry.amount,input.entry.entryTargetIndex),boughtPremium:openingUsdEquivalent(input.entry.bought.priceBtcPerContract*input.entry.amount,input.entry.entryTargetIndex),openingFees:openingUsdEquivalent(input.entry.openingFeesBtc,input.entry.entryTargetIndex),grossEntry:openingUsdEquivalent(input.entry.grossSpreadBtc,input.entry.entryTargetIndex),netOpening:openingUsdEquivalent(input.entry.netOpeningCashFlowBtc,input.entry.entryTargetIndex),theoreticalMaximumLossAtEntryIndex:openingUsdEquivalent(Math.max(0,(input.spread.actualWidth??input.spread.targetWidth)/input.entry.entryTargetIndex*input.entry.amount-input.entry.netOpeningCashFlowBtc),input.entry.entryTargetIndex),estimatedOpeningBalance:openingUsdEquivalent(input.entry.bought.priceBtcPerContract*input.entry.amount+input.entry.openingFeesBtc,input.entry.entryTargetIndex)}:undefined,estimatedEntry:input.entry,estimatedPath:input.path??[],estimatedOutcomes:input.outcomes??[]};}
